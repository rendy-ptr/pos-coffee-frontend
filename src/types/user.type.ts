export interface IUserData {
  id: string;
  name: string;
  email: string;
  role: string;
  isActive: boolean;
  profilePicture?: string;
  phone?: string;
  createdAt: string;
  updatedAt: string;
}
